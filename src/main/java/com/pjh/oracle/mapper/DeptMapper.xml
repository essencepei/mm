<?xml version="1.0" encoding="UTF-8" ?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pjh.dao.DeptMapper">
    <!-- 查询所有tree -->  
    <select id="queryTree" resultType="Dept" >  
         select * from pro01_dept where pid = #{id,jdbcType=VARCHAR}
    </select>
</mapper>